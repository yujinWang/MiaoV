1、点击登录用户名密码格式校验
2、因为我们是在Login.vue组件中点击登录获取的用户名和对应的菜单信息，而需要在其他某个页面使用。
   项目结构设计各不相同，可能会相隔很远，于是可以将这些信息保存在vuex中，当我们登录时候通过
   this.$store.commit("事件名",要保存的信息)的方式将用户名和用户对应的权限菜单信息保存在
   vuex和sessionStorage中
3、在store文件state中创建对应的变量用户名username和对应菜单信息menuList